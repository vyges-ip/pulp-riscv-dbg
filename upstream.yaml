schema_version: "vyges-upstream-v1"
upstream:
  # `repository` was never a valid type; the reusable sync workflow reads
  # `.upstream.repo // .upstream.source` for every type except monorepo, so this repo has
  # always BEHAVED as standalone. Naming it so is a no-op for the sync and makes the
  # manifest validate.
  type: "standalone"
  repo: https://github.com/pulp-platform/riscv-dbg
  commit: 643607e19df3435cfd77cc56143e2637f5900a6e
  extracted_at: "2026-04-16T04:22:21Z"
import:
  rtl:
    - path: "src"
      map_to: "rtl"
    - path: "debug_rom"
      map_to: "rtl/debug_rom"
  license:
    - path: "LICENSE"
    - path: "LICENSE.SiFive"
  readme:
    - path: "README.md"
license:
  spdx: SHL-0.51
  notes: |
    Solderpad Hardware License 0.51 — explicit Apache-2.0 opt-in clause in the
    license grants Licensees the option to treat Work as Apache 2.0 licensed.
    Compatible with Vyges Apache-2.0 catalog policy.
    Debug ROM (debug_rom/*.sv) originates from SiFive under LICENSE.SiFive
    (BSD-3-Clause-style) — retained per upstream convention.
  file: LICENSE
metadata_locked: true
